<Application x:Class="FerreteriaPOS.UI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:FerreteriaPOS.UI"
             xmlns:converters="clr-namespace:FerreteriaPOS.UI.Converters">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Converters -->
            <converters:StockBajoConverter x:Key="StockBajoConverter"/>
            <converters:IntToVisibilityConverter x:Key="IntToVisibilityConverter"/>
            
            <!-- Estilos de botones de función -->
            <Style x:Key="FunctionKeyButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="#E6E6E6"/>
                <Setter Property="Foreground" Value="Black"/>
                <Setter Property="FontWeight" Value="Normal"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="10,5"/>
            </Style>
            
            <Style x:Key="ActiveFunctionKeyButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="#2E8B57"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="10,5"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
